# deps / logs
node_modules/
*.log
.DS_Store
Thumbs.db

# runtime data (never publish)
wake/tasks.json
wake/wake.json
*.corrupt-*

# bt-skin: 壁纸素材与运行时状态不进仓库（私人图片 / 第三方素材都不该提交）
bt-skin/assets/
bt-skin/runtime.json
bt-skin/deploy-manifest.json
bt-skin/diag.json

# secrets
*.secret
totp-secret.txt
token.txt

# 补丁器留下的时间戳备份（本地用，不入库）
*.bak-*
.commit-msg.txt
